<Page x:Class="Events.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
    Title="Home">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="185"/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        <ListBox Style="{StaticResource videosListStyle}"/>
        <TextBlock Style="{StaticResource descriptionTextStyle}" />
        <Button Click="View_Click" Style="{StaticResource viewVideoButtonStyle}" />

    </Grid>
</Page>
