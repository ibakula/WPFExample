<Page x:Class="EventsApp.View.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EventsApp.View"
      xmlns:ViewModel="clr-namespace:EventsApp.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Home">

    <Page.DataContext>
        <ViewModel:HomeViewModel />
    </Page.DataContext>

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="185"/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding GetTitlesList, UpdateSourceTrigger=PropertyChanged}" SelectedIndex="{Binding Selection, Mode=TwoWay}" Style="{StaticResource videosListStyle}" />
        <TextBlock Text="{Binding GetDescription, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource descriptionTextStyle}" />
        <Button Command="{Binding ShowNews}" Style="{StaticResource viewVideoButtonStyle}" />
    </Grid>
</Page>
